import 'package:flutter/material.dart';

class TrainingRepeatSentencePage extends StatelessWidget {
  const TrainingRepeatSentencePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const SizedBox.shrink(), // TODO: Продовжити
    );
  }
}
